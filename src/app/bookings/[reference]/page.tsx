/* eslint-disable @typescript-eslint/no-explicit-any */
import { createHash } from "crypto";import { notFound,redirect } from "next/navigation";import { CheckCircle2,Clock3,XCircle } from "lucide-react";import { SiteFooter,SiteHeader } from "@/components/site-chrome";import { currentIdentity } from "@/lib/auth";import { getDb } from "@/lib/mongodb";
export default async function BookingStatusPage({params,searchParams}:{params:Promise<{reference:string}>;searchParams:Promise<{token?:string}>}){const{reference}=await params;const{token}=await searchParams;const identity=await currentIdentity();const db=await getDb();const query:any={reference};if(!identity&&token)query.publicAccessTokenHash=createHash("sha256").update(token).digest("hex");else if(identity&&!identity.platformAdmin)query.userId=identity.userId;else if(!identity)redirect(`/sign-in?redirect_url=/bookings/${reference}`);const booking=await db.collection("bookings").findOne(query);if(!booking)notFound();const Icon=booking.status==="confirmed"||booking.status==="completed"?CheckCircle2:booking.status==="pending"?Clock3:XCircle;return <main><SiteHeader/><article className="mx-auto max-w-2xl px-4 py-20 text-center"><Icon className="mx-auto text-[var(--gold-deep)]" size={54}/><p className="eyebrow mt-5">Booking {booking.reference}</p><h1 className="mt-3 font-serif text-5xl capitalize text-[var(--ink)]">{booking.status}</h1><p className="mt-4 text-[var(--muted)]">{booking.checkIn} to {booking.checkOut} · {booking.nights} nights · {booking.guests} guests</p><div className="mt-8 border border-[var(--line)] bg-[var(--cream)] p-6 text-left"><h2 className="font-serif text-2xl">Status history</h2><div className="mt-4 divide-y">{(booking.history||[]).map((item:any,index:number)=><div key={index} className="flex justify-between py-3 text-sm"><span className="capitalize">{item.status}</span><time className="text-[var(--muted)]">{new Date(item.at).toLocaleString("en-RW")}</time></div>)}</div></div></article><SiteFooter/></main>}
