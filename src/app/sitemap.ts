import type { MetadataRoute } from "next";import { destinations,offers } from "@/lib/editorial";import { listPublishedHotels } from "@/lib/platform-data";
export default async function sitemap():Promise<MetadataRoute.Sitemap>{const base="https://stayrwanda.com";const staticRoutes=["","/stays","/residences","/hotels","/destinations","/offers","/help","/safety","/cancellation","/contact","/about","/privacy","/terms"];const hotels=await listPublishedHotels();return[...staticRoutes.map(route=>({url:`${base}${route}`,lastModified:new Date(),changeFrequency:"weekly" as const,priority:route===""?1:.7})),...hotels.map(h=>({url:`${base}/hotels/${h.slug}`,lastModified:new Date(h.updatedAt),changeFrequency:"daily" as const,priority:.8})),...destinations.map(d=>({url:`${base}/destinations/${d.slug}`,lastModified:new Date(),changeFrequency:"weekly" as const,priority:.6})),...offers.map(o=>({url:`${base}/offers/${o.slug}`,lastModified:new Date(),changeFrequency:"weekly" as const,priority:.6}))]}
