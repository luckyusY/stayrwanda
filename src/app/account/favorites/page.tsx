import { Heart } from "lucide-react";import { redirect } from "next/navigation";import { AccountShell,EmptyState } from "@/components/account-shell";import { HotelCard } from "@/components/hotel-card";import { currentIdentity } from "@/lib/auth";import { getDb } from "@/lib/mongodb";import type { Hotel } from "@/lib/platform-types";
export default async function FavoritesPage(){const identity=await currentIdentity();if(!identity)redirect("/sign-in");const db=await getDb();const favs=await db.collection("favorites").find({userId:identity.userId}).toArray();const ids=favs.map(f=>f.hotelId).filter(Boolean);const{ObjectId}=await import("mongodb");const objectIds=ids.filter(ObjectId.isValid).map(id=>new ObjectId(id));const rows=objectIds.length?await db.collection("hotels").find({_id:{$in:objectIds},status:"published"}).toArray():[];const hotels=rows.map(row=>({...row,id:String(row._id)} as unknown as Hotel));return <AccountShell title="Saved properties">{hotels.length?<div className="grid gap-5 sm:grid-cols-2">{hotels.map(h=><HotelCard key={h.id} hotel={h}/>)}</div>:<EmptyState icon={Heart} title="Save properties you like" copy="Tap the heart on a stay and compare your favourites here later." action="Browse properties" href="/stays"/>}</AccountShell>}
