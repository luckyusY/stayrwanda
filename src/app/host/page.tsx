import Link from "next/link"; import { CrmHeading, Empty, Pill, Stat } from "@/components/crm-ui"; import { hostContext } from "@/lib/host-context"; import { hostDashboardData } from "@/lib/host-data";
export default async function HostDashboard(){const{organizationId}=await hostContext();const data=await hostDashboardData(organizationId!);const pending=data.bookings.filter(x=>x.status==="pending");return <><CrmHeading eyebrow="Operations" title="Overview" copy="Today’s hospitality activity across your organization."/><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Stat label="Properties" value={data.hotels.length} detail={`${data.hotels.filter(x=>x.status==="published").length} live`}/><Stat label="Pending requests" value={pending.length} detail="Awaiting action"/><Stat label="Upcoming arrivals" value={data.bookings.filter(x=>x.status==="confirmed"&&x.checkIn>=new Date().toISOString().slice(0,10)).length} detail="Confirmed stays"/><Stat label="Unit inventory" value={data.units.reduce((n,x)=>n+Number(x.quantity||0),0)} detail="Across all unit types"/></div><section className="mt-7 border border-[#e1ded5] bg-white"><div className="flex items-center justify-between border-b p-5"><h2 className="font-serif text-2xl text-[var(--ink)]">Recent requests</h2><Link href="/host/reservations" className="text-xs font-semibold uppercase tracking-wider text-[var(--gold-deep)]">View all</Link></div>{data.bookings.length?<div className="divide-y">{data.bookings.slice(0,6).map(x=><div key={x.id} className="flex items-center gap-4 p-4"><div className="min-w-0 flex-1"><strong className="block truncate text-sm">{x.guest?.name||x.guestName}</strong><span className="text-xs text-[var(--muted)]">{x.reference} · {x.checkIn} to {x.checkOut}</span></div><Pill value={x.status}/></div>)}</div>:<Empty title="No booking requests yet" copy="New guest requests will appear here."/>}</section></>}
