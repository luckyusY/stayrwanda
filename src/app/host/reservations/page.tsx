import { CrmHeading,Empty } from "@/components/crm-ui";import { HostReservations } from "@/components/host-reservations";import { hostContext } from "@/lib/host-context";import { hostDashboardData } from "@/lib/host-data";
export default async function Reservations(){const{organizationId}=await hostContext();const{bookings}=await hostDashboardData(organizationId!);return <><CrmHeading eyebrow="Guest operations" title="Reservations" copy="Review, confirm and manage inventory-backed booking requests."/>{bookings.length?<HostReservations bookings={bookings}/>:<Empty title="No reservations" copy="Booking requests will appear here."/>}</>}
