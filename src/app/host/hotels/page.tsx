import Image from "next/image"; import Link from "next/link"; import { CrmHeading, Empty, Pill } from "@/components/crm-ui"; import { hostContext } from "@/lib/host-context"; import { listHotelsForOrganization } from "@/lib/platform-data";
export default async function HostHotels(){const{organizationId}=await hostContext();const hotels=await listHotelsForOrganization(organizationId!);return <><CrmHeading eyebrow="Portfolio" title="Hotels & residences" copy="Manage public profiles, templates and publication status." action={<Link href="/host/onboarding" className="bg-[var(--ink)] px-5 py-3 text-xs uppercase tracking-wider text-white">Add property</Link>}/>{hotels.length?<div className="grid gap-5 sm:grid-cols-2 xl:grid-cols-3">{hotels.map(h=><article key={h.id} className="border border-[#e1ded5] bg-white"><div className="relative aspect-[16/9] bg-[#eee]">{h.heroImage&&<Image src={h.heroImage} alt={h.name} fill className="object-cover"/>}</div><div className="p-5"><div className="flex justify-between gap-2"><h2 className="font-serif text-2xl text-[var(--ink)]">{h.name}</h2><Pill value={h.status}/></div><p className="mt-2 text-xs text-[var(--muted)]">{h.category} · {h.template} template</p><div className="mt-5 flex gap-2"><Link href={`/host/hotels/${h.id}`} className="bg-[var(--ink)] px-4 py-2 text-xs text-white">Manage</Link>{h.status==="published"&&<Link href={`/hotels/${h.slug}`} target="_blank" className="border border-[#ccc] px-4 py-2 text-xs">View live</Link>}</div></div></article>)}</div>:<Empty title="Create your first property" copy="The onboarding wizard guides you from basics through review."/>}</>}
