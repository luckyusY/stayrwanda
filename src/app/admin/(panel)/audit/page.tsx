import { AdminShell } from "@/components/admin-shell";import { getDb } from "@/lib/mongodb";
export default async function Audit(){const db=await getDb();const rows=await db.collection("auditLogs").find({}).sort({createdAt:-1}).limit(500).toArray();return <AdminShell title="Audit log" subtitle="Actor, tenant, record and before/after mutation history"><div className="overflow-x-auto rounded-2xl border bg-white"><table className="w-full min-w-[900px] text-left text-sm"><thead className="bg-[#f9fafb] text-xs uppercase tracking-wide text-[#667085]"><tr><th className="p-4">Time</th><th>Actor</th><th>Action</th><th>Organization</th><th>Record</th></tr></thead><tbody className="divide-y">{rows.map(r=><tr key={String(r._id)}><td className="p-4 text-xs">{new Date(r.createdAt).toLocaleString("en-RW")}</td><td>{r.actorEmail||r.actorUserId}</td><td className="font-semibold">{r.action}</td><td>{r.organizationId||"Platform"}</td><td>{r.recordType} · {r.recordId}</td></tr>)}</tbody></table></div></AdminShell>}
