"use client";import { useState } from "react";import { useRouter } from "next/navigation";import type { Hotel } from "@/lib/platform-types";import { Loader2 } from "lucide-react";
export function HotelEditor({hotel}:{hotel:Hotel}){const router=useRouter();const[form,setForm]=useState({name:hotel.name,description:hotel.description,template:hotel.template,amenities:hotel.amenities.join(", ")});const[busy,setBusy]=useState("");const[message,setMessage]=useState("");async function save(){setBusy("save");const res=await fetch(`/api/hotels/${hotel.id}`,{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({...form,amenities:form.amenities.split(",").map(x=>x.trim()).filter(Boolean)})});const data=await res.json();setMessage(res.ok?"Draft saved.":data.error);setBusy("");router.refresh()}async function submit(){setBusy("submit");const res=await fetch(`/api/hotels/${hotel.id}`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({action:"submit"})});const data=await res.json();setMessage(res.ok?"Submitted for StayRwanda review.":data.error);setBusy("");router.refresh()}return <div className="grid gap-6 lg:grid-cols-[1fr_320px]"><section className="space-y-5 border border-[#e1ded5] bg-white p-6"><label className="block text-sm font-semibold">Profile name<input value={form.name} onChange={e=>setForm(x=>({...x,name:e.target.value}))} className="mt-2 min-h-12 w-full border px-3 font-normal"/></label><label className="block text-sm font-semibold">Template<select value={form.template} onChange={e=>setForm(x=>({...x,template:e.target.value as Hotel["template"]}))} className="mt-2 min-h-12 w-full border bg-white px-3 font-normal"><option value="classic">Classic Hospitality</option><option value="editorial">Editorial Residence</option><option value="modern">Modern Urban</option></select></label><label className="block text-sm font-semibold">Amenities<input value={form.amenities} onChange={e=>setForm(x=>({...x,amenities:e.target.value}))} className="mt-2 min-h-12 w-full border px-3 font-normal"/></label><label className="block text-sm font-semibold">Property story<textarea value={form.description} onChange={e=>setForm(x=>({...x,description:e.target.value}))} className="mt-2 min-h-48 w-full border p-3 font-normal"/></label><button onClick={save} disabled={!!busy} className="flex items-center gap-2 bg-[var(--ink)] px-6 py-3 text-xs uppercase tracking-wider text-white">{busy==="save"&&<Loader2 size={15} className="animate-spin"/>}Save draft</button>{message&&<p className="text-sm text-[var(--muted)]">{message}</p>}</section><aside className="h-fit border border-[#e1ded5] bg-white p-6"><p className="text-xs uppercase tracking-wider text-[var(--muted)]">Publication</p><strong className="mt-2 block font-serif text-2xl capitalize">{hotel.status}</strong><p className="mt-3 text-sm leading-6 text-[var(--muted)]">Submitting sends this profile to the platform review queue without changing the live version.</p><button onClick={submit} disabled={!!busy||hotel.status==="pending"} className="mt-5 flex w-full items-center justify-center gap-2 bg-[var(--gold)] px-5 py-3 text-xs uppercase tracking-wider text-white disabled:opacity-50">{busy==="submit"&&<Loader2 size={15} className="animate-spin"/>}Submit for review</button></aside></div>}
