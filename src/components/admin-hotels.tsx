/* eslint-disable @typescript-eslint/no-explicit-any */
"use client";import Image from "next/image";import Link from "next/link";import { useMemo,useState } from "react";import { useRouter } from "next/navigation";import { Pill } from "@/components/crm-ui";
export function AdminHotels({hotels}:{hotels:Array<Record<string,any>>}){const router=useRouter();const[query,setQuery]=useState("");const[status,setStatus]=useState("all");const[busy,setBusy]=useState("");const rows=useMemo(()=>hotels.filter(h=>(status==="all"||h.status===status)&&`${h.name} ${h.slug} ${h.location?.neighborhood}`.toLowerCase().includes(query.toLowerCase())),[hotels,query,status]);async function publish(id:string){setBusy(id);await fetch(`/api/hotels/${id}`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({action:"publish"})});router.refresh();setBusy("")}return <><div className="mb-4 flex gap-3"><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search property or neighbourhood" className="min-h-11 flex-1 rounded-lg border px-3"/><select value={status} onChange={e=>setStatus(e.target.value)} className="rounded-lg border bg-white px-3"><option value="all">All statuses</option><option>pending</option><option>published</option><option>draft</option><option>rejected</option></select></div><div className="overflow-x-auto rounded-2xl border bg-white"><table className="w-full min-w-[850px] text-left text-sm"><thead className="bg-[#f9fafb] text-xs uppercase tracking-wide text-[#667085]"><tr><th className="p-4">Property</th><th>Organization</th><th>Template</th><th>Status</th><th>Actions</th></tr></thead><tbody className="divide-y">{rows.map(h=><tr key={h.id} className={busy===h.id?"opacity-50":""}><td className="p-4"><div className="flex items-center gap-3"><div className="relative size-12 overflow-hidden rounded bg-[#eee]">{h.heroImage&&<Image src={h.heroImage} alt="" fill className="object-cover"/>}</div><div><strong>{h.name}</strong><small className="block text-[#667085]">{h.location?.neighborhood} · {h.category}</small></div></div></td><td>{h.organizationName||h.organizationId}</td><td className="capitalize">{h.template}</td><td><Pill value={h.status}/></td><td className="space-x-2">{h.status==="pending"&&<button onClick={()=>publish(h.id)} className="rounded border border-green-700 px-3 py-1.5 text-xs font-bold text-green-700">Approve & publish</button>}<Link href={`/hotels/${h.slug}`} target="_blank" className="rounded border px-3 py-1.5 text-xs">Preview</Link></td></tr>)}</tbody></table></div></>}
