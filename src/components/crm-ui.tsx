export function CrmHeading({eyebrow,title,copy,action}:{eyebrow?:string;title:string;copy:string;action?:React.ReactNode}){return <div className="mb-7 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div>{eyebrow&&<p className="text-[10px] font-semibold uppercase tracking-[.18em] text-[var(--gold-deep)]">{eyebrow}</p>}<h1 className="mt-1 font-serif text-4xl font-semibold text-[var(--ink)]">{title}</h1><p className="mt-2 text-sm text-[var(--muted)]">{copy}</p></div>{action}</div>}
export function Stat({label,value,detail}:{label:string;value:string|number;detail:string}){return <div className="border border-[#e1ded5] bg-white p-5"><span className="text-xs text-[var(--muted)]">{label}</span><strong className="mt-2 block font-serif text-3xl text-[var(--ink)]">{value}</strong><small className="mt-1 block text-[var(--muted)]">{detail}</small></div>}
export function Empty({title,copy}:{title:string;copy:string}){return <div className="border border-[#e1ded5] bg-white p-10 text-center"><h2 className="font-serif text-2xl text-[var(--ink)]">{title}</h2><p className="mt-2 text-sm text-[var(--muted)]">{copy}</p></div>}
export function Pill({value}:{value:string}){const live=["published","confirmed","active","completed"].includes(value);const pending=["pending","invited","draft"].includes(value);return <span className={`rounded-full px-2.5 py-1 text-[10px] font-semibold uppercase tracking-wider ${live?"bg-[#e8f5ec] text-[#257744]":pending?"bg-[#fff4df] text-[#9a6511]":"bg-[#f2eeee] text-[#855]"}`}>{value}</span>}
